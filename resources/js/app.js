import Chart from 'chart.js/auto';

// Make Chart globally available for Livewire @script directives
window.Chart = Chart;

// App initialization
document.addEventListener('DOMContentLoaded', () => {
    console.log('Hadirku - Aplikasi Presensi Digital');
});
